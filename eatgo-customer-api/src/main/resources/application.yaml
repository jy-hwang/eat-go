spring:
  application:
    name: eatgo-customer
  datasource:
    url: ${H2_REAL_URL}
  jpa:
    hibernate:
      ddl-auto: update

server:
  port: ${CUSTOMER_PORT}

---
spring:
  datasource:
    url: ${H2_TEST_URL}
  config:
    activate:
      on-profile: test
